.context_common_detail
  padding-top: 56px!important
  .story_detail_context
    responsiveStyle(padding, 0, 0 50px)
    position: relative
    .child_title
      text-align: right
      responsiveStyle(margin-right, 0, 100px)
      font-size: 14px
      color: #999
      b
        font-weight: normal
    .to_back
      position: absolute
      responsiveStyle(width, 3rem, 60px)
      responsiveStyle(height, 3rem, 60px)
      right: 0
      responsiveStyle(bottom, -2rem, -60px)
      border: none
      border-radius: 100%
      cursor: pointer
      outline: none
    h2
      line-height: 60px
      text-align: center
    img
      display: block
      responsiveStyle(width, 100%, 800px)
      responsiveStyle(height, 222px, 400px)
      margin: 20px auto
    p
      text-indent: 2em
      